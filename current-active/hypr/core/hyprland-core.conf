# --- Arch Nexus Core Configuration ---
# Enthält Monitore, Inputs, Autostarts und globale Hotkeys

# --- Monitor Configuration ---
monitor = DP-2, 1920x1080@60, 0x0, 1
workspace = 1, monitor:DP-2
workspace = 2, monitor:DP-2
workspace = 3, monitor:DP-2

monitor = DP-1, 2560x1440@200, 1920x0, 1.33, vrr, 1
workspace = 4, monitor:DP-1
workspace = 5, monitor:DP-1
workspace = 6, monitor:DP-1

monitor = HDMI-A-1, 1920x1080@60, auto, 1
workspace = 7, monitor:HDMI-A-1
workspace = 8, monitor:HDMI-A-1
workspace = 9, monitor:HDMI-A-1

# --- Input Settings ---
input {
    kb_layout=de
    follow_mouse=1
    sensitivity=0 
    touchpad {
        natural_scroll=no
    }
}

# --- Windows-Style AltGr Mapping (via wtype) ---
bind = CTRL ALT, Q, exec, wtype "@"
bind = CTRL ALT, E, exec, wtype "€"
bind = CTRL ALT, 2, exec, wtype "²"
bind = CTRL ALT, 3, exec, wtype "³"
bind = CTRL ALT, M, exec, wtype "µ"
bind = CTRL ALT, 7, exec, wtype "{"
bind = CTRL ALT, 8, exec, wtype "["
bind = CTRL ALT, 9, exec, wtype "]"
bind = CTRL ALT, 0, exec, wtype "}"
bind = CTRL ALT, backslash, exec, wtype "\\"

# --- Standard Hotkeys ---
$mod=SUPER
$browser = firefox

bind=$mod,Return,exec,kitty
bind=$mod,B,exec,$browser
bind=$mod,SPACE,exec,~/.config/rofi/launch.sh
bind=$mod,W,exec,~/.config/rofi/wallpaper-launcher.sh
bind=$mod,T,exec,~/.config/rofi/theme-switcher.sh
bind=$mod,Escape,exec,~/.local/bin/nexus-menu
bind=$mod,Q,killactive,
bind=$mod,M,exit,
bind=$mod,E,exec,dolphin
bind=$mod,I,exec,kwrite
bind=$mod,R,exec,flatpak run com.heroicgameslauncher.hgl
bind=$mod,L,exec,hyprlock

# --- Screenshot (Fixed: Only one bind) ---
bind=$mod SHIFT,S,exec,bash -c 'grim -g "$(slurp)" - | wl-copy && notify-send "Screenshot" "Region copied to clipboard"'

# --- Window Management ---
bind=$mod,Left,focuswindow,l
bind=$mod,Right,focuswindow,r
bind=$mod,Up,focuswindow,u
bind=$mod,Down,focuswindow,d
bind=$mod SHIFT,Left,swapwindow,l
bind=$mod SHIFT,Right,swapwindow,r
bind=$mod SHIFT,Up,swapwindow,u
bind=$mod SHIFT,Down,swapwindow,d
bind=$mod CONTROL SHIFT,Left,movewindow,l
bind=$mod CONTROL SHIFT,Right,movewindow,r
bind=$mod CONTROL SHIFT,Up,movewindow,u
bind=$mod CONTROL SHIFT,Down,movewindow,d
bind=$mod,P,togglesplit,
bind=$mod,V,togglefloating,
bind=$mod,F,fullscreen,
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# --- Workspace Management ---
bind=$mod,1,workspace,1
bind=$mod,2,workspace,2
bind=$mod,3,workspace,3
bind=$mod,4,workspace,4
bind=$mod,5,workspace,5
bind=$mod,6,workspace,6
bind=$mod,7,workspace,7
bind=$mod,8,workspace,8
bind=$mod,9,workspace,9

# --- Media & Brightness ---
bindl=,XF86AudioPlay,exec,playerctl play-pause
bindle=,XF86AudioRaiseVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ +5%
bindle=,XF86AudioLowerVolume,exec,pactl set-sink-volume @DEFAULT_SINK@ -5%
bindle=,XF86MonBrightnessUp,exec,brightnessctl set +10%
bindle=,XF86MonBrightnessDown,exec,brightnessctl set 10%-

# --- Automation & Services (VibeFlow) ---
bind = CONTROL SHIFT, space, exec, touch $HOME/vibeflow.trigger
exec-once = $HOME/Documents/VibeFlow/venv/bin/python -X utf8 $HOME/Documents/VibeFlow/main.py

# --- Startup ---
exec-once = ~/.config/hypr/scripts/xdg-portal-hyprland
exec-once = waybar
exec-once = swww-daemon
exec-once = hypridle
exec-once = vesktop --use-gl=wayland --enable-features=UseOzonePlatform --ozone-platform=wayland --start-minimized
